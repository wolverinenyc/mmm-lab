[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "mmm-lab"
version = "0.1.0"
description = "Simulated and real-world MMM experiments for learning and content"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
